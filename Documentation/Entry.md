# Entry

A virtual file provider entry

``` swift
public enum Entry
```

## Enumeration Cases

### `file`

A file entry

``` swift
case file(: VirtualFile)
```

### `directory`

A directory entry

``` swift
case directory(: VirtualDirectory)
```
