import XCTest
@testable import VirtualFileProvider


final class VirtualFileProviderTests: XCTestCase {
    // Tests are not useful yet because we define an API only

    static var allTests: [() -> Void] = []
}
